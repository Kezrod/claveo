---
---

<header class="w-full flex items-center justify-end px-8 py-4 bg-bg text-text">
  <a 
    href="/" 
    class="bg-accent hover:bg-accent-hover transition px-4 py-2 rounded-lg font-semibold text-white text-sm"
  >
    Volver al inicio
  </a>
</header>

<!-- 📦 Contenedor principal -->
<div class="min-h-screen flex items-center justify-center bg-bg text-text pb-12">
  <div id="authContainer" class="bg-white text-black rounded-xl shadow-xl p-8 w-full max-w-md border border-border transition-all duration-300">
    <!-- 🧑‍💻 Login por defecto -->
    <div id="loginForm">
      <h1 class="text-3xl font-extrabold text-center mb-6">Bienvenido a Claveo</h1>
      <p class="text-center text-sm text-gray-600 mb-8">Inicia sesión para continuar</p>

      <form class="flex flex-col gap-5">
        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-semibold mb-2">Correo electrónico</label>
          <input
            type="email"
            id="email"
            placeholder="tucorreo@ejemplo.com"
            class="w-full px-4 py-2 rounded-lg border border-border focus:outline-none focus:ring-2 focus:ring-accent placeholder-gray-400"
            required
          />
        </div>

        <!-- Contraseña -->
        <div>
          <label for="password" class="block text-sm font-semibold mb-2">Contraseña</label>
          <input
            type="password"
            id="password"
            placeholder="••••••••"
            class="w-full px-4 py-2 rounded-lg border border-border focus:outline-none focus:ring-2 focus:ring-accent placeholder-gray-400"
            required
          />
        </div>

        <!-- Botón -->
        <button
          type="submit"
          class="w-full bg-accent hover:bg-accent-hover text-white font-semibold py-2 rounded-lg transition"
        >
          Iniciar sesión
        </button>
      </form>

      <!-- Links -->
      <div class="text-center mt-6 space-y-2">
        <button id="showRegister" class="block w-full text-accent hover:underline text-sm font-semibold">
          Crear cuenta
        </button>
        <a href="#" class="block text-gray-500 hover:underline text-xs">
          ¿Olvidaste tu contraseña?
        </a>
      </div>
    </div>

    <!-- 🆕 Formulario de registro (oculto por defecto) -->
    <div id="registerForm" class="hidden">
      <h1 class="text-3xl font-extrabold text-center mb-6">Crear cuenta</h1>
      <p class="text-center text-sm text-gray-600 mb-8">Completa tus datos para registrarte</p>

      <form class="flex flex-col gap-5">
        <!-- Nombre -->
        <div>
          <label for="nombre" class="block text-sm font-semibold mb-2">Nombre completo</label>
          <input
            type="text"
            id="nombre"
            placeholder="Tu nombre completo"
            class="w-full px-4 py-2 rounded-lg border border-border focus:outline-none focus:ring-2 focus:ring-accent placeholder-gray-400"
            required
          />
        </div>

        <!-- Fecha de nacimiento -->
        <div>
          <label for="fecha" class="block text-sm font-semibold mb-2">Fecha de nacimiento</label>
          <input
            type="date"
            id="fecha"
            class="w-full px-4 py-2 rounded-lg border border-border focus:outline-none focus:ring-2 focus:ring-accent"
            required
          />
        </div>

        <!-- Correo -->
        <div>
          <label for="emailRegister" class="block text-sm font-semibold mb-2">Correo electrónico</label>
          <input
            type="email"
            id="emailRegister"
            placeholder="tucorreo@ejemplo.com"
            class="w-full px-4 py-2 rounded-lg border border-border focus:outline-none focus:ring-2 focus:ring-accent placeholder-gray-400"
            required
          />
        </div>

        <!-- Contraseña -->
        <div>
          <label for="passwordRegister" class="block text-sm font-semibold mb-2">Contraseña</label>
          <input
            type="password"
            id="passwordRegister"
            placeholder="••••••••"
            class="w-full px-4 py-2 rounded-lg border border-border focus:outline-none focus:ring-2 focus:ring-accent placeholder-gray-400"
            required
          />
        </div>

        <!-- Confirmar contraseña -->
        <div>
          <label for="confirmPassword" class="block text-sm font-semibold mb-2">Confirmar contraseña</label>
          <input
            type="password"
            id="confirmPassword"
            placeholder="••••••••"
            class="w-full px-4 py-2 rounded-lg border border-border focus:outline-none focus:ring-2 focus:ring-accent placeholder-gray-400"
            required
          />
        </div>

        <button
          type="submit"
          class="w-full bg-accent hover:bg-accent-hover text-white font-semibold py-2 rounded-lg transition"
        >
          Crear cuenta
        </button>
      </form>

      <div class="text-center mt-6">
        <button id="showLogin" class="text-accent hover:underline text-sm font-semibold">
          Volver al inicio de sesión
        </button>
      </div>
    </div>
  </div>
</div>

<!-- 📜 Script para alternar formularios -->
<script is:inline>
  const loginForm = document.getElementById("loginForm");
  const registerForm = document.getElementById("registerForm");
  const showRegisterBtn = document.getElementById("showRegister");
  const showLoginBtn = document.getElementById("showLogin");

  showRegisterBtn?.addEventListener("click", () => {
    loginForm.classList.add("hidden");
    registerForm.classList.remove("hidden");
  });

  showLoginBtn?.addEventListener("click", () => {
    registerForm.classList.add("hidden");
    loginForm.classList.remove("hidden");
  });
</script>
